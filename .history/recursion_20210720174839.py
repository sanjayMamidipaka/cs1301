def dictCreation(keys, vals):

    if len(keys) == 0 and len(vals) == 0:
        return
    key = keys[0]
    val = vals[0]

    remainingKeys = keys[1:]
    remainingVals = vals[1:]




keys = ['a', 'b', 'c']
values = [1,2,3]